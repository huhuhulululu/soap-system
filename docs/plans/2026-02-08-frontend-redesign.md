# 前端改进设计方案 v1.0

## 概述

基于新的检查规则体系（48 条规则，19 CRITICAL / 16 HIGH / 13 MEDIUM），重构前端展示和交互。

核心变更：**有 CRITICAL 即 FAIL**

---

## 一、规则严重度分布

### CRITICAL（19 条）— 任意一条触发即 FAIL

| ID | 规则名 |
|----|--------|
| DOC01 | 缺少初诊记录 |
| TX05 | 舌脉与 IE 基线一致 |
| TX06 | 不应出现 goals |
| DX01 | ICD→bodyPart 匹配 |
| DX02 | ICD 跨 visit 一致 |
| DX03 | ICD 编码存在 |
| DX04 | ICD laterality 一致 |
| CPT01 | CPT 编码存在 |
| CPT02 | CPT↔电刺激匹配 |
| CPT03 | CPT↔时间 units 匹配 |
| CPT04 | IE/TX CPT 类型正确 |
| O9 | ROM movement belongs to bodyPart |
| A5 | localPattern 跨 visit 一致 |
| P1 | Needle gauge vs bodyPart |
| P2 | Acupoints 数量合理 |
| X1 | Pain→Severity→Tightness→Tenderness 链 |
| X2 | Pain→ROM→Strength 链 |
| X3 | Pattern→Tongue/Pulse 链 |
| X4 | Pacemaker→Electrical Stimulation |

### HIGH（16 条）— 每条扣 15 分

| ID | 规则名 |
|----|--------|
| IE01 | pain→severity 映射 |
| IE05 | 短期目标合理 |
| IE06 | 长期目标合理 |
| IE07 | TCM diagnosis 完整 |
| IE08 | P 段 needle protocol |
| V01 | pain 不回升 |
| V02 | tenderness 不回升 |
| V03 | tightness 不恶化 |
| V04 | spasm 不回升 |
| V05 | ROM 不下降 |
| V09 | 穴位跨 TX 稳定 |
| S7 | muscleWeaknessScale vs pain |
| O1 | ROM degrees vs pain |
| O2 | ROM limitation label vs degrees |
| O3 | Strength vs ROM limitation |
| O8 | Muscles belong to bodyPart |

### MEDIUM（13 条）— 每条扣 8 分

| ID | 规则名 |
|----|--------|
| IE02 | pain→tenderness 合理 |
| IE03 | pain→ROM limitation |
| IE04 | 舌脉→证型一致 |
| TX01 | pain→severity 映射 |
| TX02 | pain→tenderness 合理 |
| TX03 | symptomChange 与 pain delta 一致 |
| TX04 | generalCondition 合理 |
| V06 | strength 不下降 |
| V07 | frequency 不增加 |
| V08 | improvement 与 pain 一致 |
| S2 | painTypes vs localPattern |
| S3 | ADL activities vs bodyPart |
| S6 | Associated symptom 单调 |

---

## 二、评分逻辑

```
有 CRITICAL 错误 → FAIL（分数 0）
无 CRITICAL 错误 → 100 - (HIGH×15 + MEDIUM×8)
  ≥80 → PASS
  ≥60 → WARNING
  <60 → FAIL
```

---

## 三、核心功能设计

| 功能 | 设计 |
|------|------|
| CRITICAL 警告 | 顶部红色横幅，点击跳转错误列表 |
| 错误列表 | 按 Visit 分组，CRITICAL 默认展开 |
| 纠正面板 | 内嵌错误卡片，上下对比（原文在上，纠正在下） |
| 趋势图表 | ApexCharts 折线图，7 指标可选（默认 Pain/ROM/Strength） |
| 批量操作 | 导出完整 CSV + 复制所有纠正（按 Section 分隔） |
| 历史记录 | 独立 Tab 页，LocalStorage 7 天自动清理 |

---

## 四、组件架构

```
frontend/src/
├── views/
│   ├── CheckerView.vue      # 主验证页（现有 App.vue 内容）
│   └── HistoryView.vue      # 历史记录页（新增）
├── components/
│   ├── CriticalBanner.vue   # 红色警告横幅（新增）
│   ├── VisitErrorGroup.vue  # 按 Visit 分组的错误列表（新增）
│   ├── CorrectionCard.vue   # 上下对比纠正卡片（新增）
│   ├── TrendChart.vue       # ApexCharts 趋势图（新增）
│   ├── IndicatorFilter.vue  # 指标复选框（新增）
│   ├── ReportPanel.vue      # 改造：整合新组件
│   └── ...existing
├── composables/
│   └── useHistory.js        # LocalStorage 历史管理（新增）
└── services/
    └── batch-exporter.js    # 批量导出（新增）
```

---

## 五、UI 交互设计

### 5.1 主界面布局

```
┌────────────────────────────────────────────────────────────┐
│  [验证]  [历史]                               AChecker v1  │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌─ 红色横幅 ─────────────────────────────────────────┐   │
│  │ ⚠ 不合格：检测到 3 个严重问题         [查看详情 ↓]  │   │
│  └────────────────────────────────────────────────────┘   │
│                                                            │
│  ┌─ 患者信息 ──────────────────────┐ ┌─ 评分 ────────┐   │
│  │ John Doe · ID: 12345 · DOB: ... │ │  0 分  FAIL   │   │
│  └─────────────────────────────────┘ └───────────────┘   │
│                                                            │
│  ┌─ 趋势图表 ─────────────────────────────────────────┐   │
│  │  ☑ Pain  ☑ ROM  ☑ Strength  ☐ Tenderness  ☐ ...    │   │
│  │                                                      │   │
│  │      8 ●──────●                                     │   │
│  │      7       ●──🔴 ← 异常点（悬停提示 + 点击跳转）   │   │
│  │      6                                              │   │
│  │     IE    TX1   TX2   TX3                          │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                            │
│  ┌─ 错误列表（按 Visit）──────────────────────────────┐   │
│  │                                                      │   │
│  │  ▼ Visit 1 (IE) - 2026-01-05         [2 个错误]     │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │ [CRITICAL] DX01: ICD→bodyPart 匹配            │  │   │
│  │  │ ICD M54.5 与主诉部位 KNEE 不匹配               │  │   │
│  │  │                                               │  │   │
│  │  │ ▼ 查看纠正                                    │  │   │
│  │  │ ┌─ 原文 ────────────────────────────────────┐ │  │   │
│  │  │ │ Assessment:                               │ │  │   │
│  │  │ │ ICD-10: M54.5 (Low back pain)            │ │  │   │
│  │  │ └───────────────────────────────────────────┘ │  │   │
│  │  │ ┌─ 纠正 ────────────────────────────────────┐ │  │   │
│  │  │ │ Assessment:                               │ │  │   │
│  │  │ │ ICD-10: M25.561 (Right knee pain)        │ │  │   │
│  │  │ │                          [复制整段 A]     │ │  │   │
│  │  │ └───────────────────────────────────────────┘ │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │                                                      │   │
│  │  ▶ Visit 2 (TX) - 2026-01-09         [1 个错误]     │   │
│  │                                                      │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                            │
│  [批量导出 CSV]  [复制所有纠正]                            │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

### 5.2 历史记录页

```
┌────────────────────────────────────────────────────────────┐
│  [验证]  [历史]                               AChecker v1  │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  历史记录（自动保留 7 天）                    [清空全部]   │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 文件名                  日期        分数   状态     │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ patient_john_doe.pdf    2026-02-08   85    PASS     │   │
│  │ patient_jane_smith.pdf  2026-02-07   0     FAIL     │   │
│  │ patient_bob_wilson.pdf  2026-02-05   72    WARNING  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                            │
│  点击行可查看详细报告                                       │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## 六、技术选型

| 项目 | 选择 | 说明 |
|------|------|------|
| 图表库 | ApexCharts (vue3-apexcharts) | Vue 友好，交互性好 |
| 路由 | Vue Router 4 | 支持验证/历史 Tab 切换 |
| 图表交互 | 悬停 Tooltip + 点击跳转 | 异常点红色高亮 |
| 复制格式 | 纯文本 | 直接粘贴到 EMR |
| 批量复制 | 按 Section 分隔 | 所有 S 段、所有 O 段... |
| 批量导出 | 完整 CSV | 含所有错误详情，每行一个错误 |
| 存储 | LocalStorage | 7 天自动清理 |
| 响应式 | 仅桌面端 | 不做移动端适配 |
| 展开策略 | CRITICAL 默认展开 | 其余折叠 |

---

## 七、新增依赖

```json
{
  "apexcharts": "^3.45.0",
  "vue3-apexcharts": "^1.4.0",
  "vue-router": "^4.x"
}
```

---

## 八、实施步骤

### Phase 1: 基础架构
- [ ] 安装依赖 (apexcharts, vue3-apexcharts, vue-router)
- [ ] 配置 Vue Router
- [ ] 创建 views/CheckerView.vue
- [ ] 创建 views/HistoryView.vue
- [ ] 更新 App.vue 为路由容器

### Phase 2: 核心组件
- [ ] CriticalBanner.vue - 红色警告横幅
- [ ] VisitErrorGroup.vue - Visit 分组错误列表
- [ ] CorrectionCard.vue - 上下对比纠正卡片
- [ ] TrendChart.vue - ApexCharts 趋势图
- [ ] IndicatorFilter.vue - 指标复选框

### Phase 3: 数据适配
- [ ] 更新 ReportPanel.vue 适配新数据结构
- [ ] 创建 useHistory.js composable
- [ ] 创建 batch-exporter.js 服务

### Phase 4: 功能整合
- [ ] 整合所有组件到 CheckerView
- [ ] 实现历史记录功能
- [ ] 实现批量导出/复制
- [ ] 测试全流程

---

## 九、数据结构适配

### 9.1 新 CheckOutput 结构

```typescript
interface CheckOutput {
  patient: PatientInfo
  summary: {
    totalVisits: number
    visitDateRange: { first: string; last: string }
    errorCount: { critical: number; high: number; medium: number; low: number; total: number }
    scoring: {
      totalScore: number  // 有 CRITICAL 时为 0
      grade: 'PASS' | 'WARNING' | 'FAIL'
      ieConsistency: number
      txConsistency: number
      timelineLogic: number
    }
  }
  timeline: TimelineEntry[]
  errors: CheckError[]
  corrections: CorrectionItem[]
}
```

### 9.2 TimelineEntry 指标

```typescript
interface TimelineEntry {
  visitDate: string
  visitIndex: number
  visitType: 'IE' | 'TX'
  indicators: {
    pain: { value: number; trend: '↓'|'→'|'↑'; ok: boolean }
    tenderness: { value: string; trend: '↓'|'→'|'↑'; ok: boolean }
    tightness: { value: string; trend: '↓'|'→'|'↑'; ok: boolean }
    spasm: { value: string; trend: '↓'|'→'|'↑'; ok: boolean }
    frequency: { value: string; trend: '↓'|'→'|'↑'; ok: boolean }
    rom: { summary: string; trend: '↓'|'→'|'↑'; ok: boolean }
    strength: { summary: string; trend: '↓'|'→'|'↑'; ok: boolean }
  }
  errors: CheckError[]
}
```

---

## 十、CSV 导出格式

### 批量导出列定义

| 列名 | 说明 |
|------|------|
| filename | 文件名 |
| patient_name | 患者姓名 |
| patient_id | 患者 ID |
| total_visits | 总就诊次数 |
| date_range | 日期范围 |
| score | 总分 |
| grade | PASS/WARNING/FAIL |
| critical_count | CRITICAL 错误数 |
| high_count | HIGH 错误数 |
| medium_count | MEDIUM 错误数 |
| error_visit | 错误所在 Visit |
| error_section | 错误所在 Section (S/O/A/P) |
| error_rule_id | 规则 ID |
| error_severity | 严重度 |
| error_message | 错误信息 |
| error_expected | 期望值 |
| error_actual | 实际值 |

---

## 更新日志

- 2026-02-08: 初始设计完成
